import type { Response, Request } from "express";

const getRestaurants = (res:Response, req:Request) =>{
    return;
}

const updateStatus = (res:Response, req:Request) =>{
    return;
}

export {
    getRestaurants,
    updateStatus
}